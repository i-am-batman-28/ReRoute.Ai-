# Backend loads `.env` from (in order; later overrides earlier):
#   ReRoute.Ai-/.env → reroute-ai/.env → reroute-ai/backend/.env
#
# Full variable list and comments: ../../.env.example (repository root)
